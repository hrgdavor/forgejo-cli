# Sync branches - `src/fg-sync.js`

Sync repositories by copying branches and tags from a source folder to a target folder.

## Usage

```bash
bun run src/fg-sync.js <source-folder> <target-folder>
```

## Environment variables

None required.